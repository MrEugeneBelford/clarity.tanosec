# Clarity by Tanosec

This is a NextJS application for AI-powered cybersecurity insights, hosted on Netlify.

To get started, take a look at src/app/page.tsx.

## Development

```bash
npm install
npm run dev
```

## Deployment

This application is configured for deployment on Netlify. The build command is `npm run build` and the publish directory is `.next`.
