import type{DisplayFinding,EvidencePanel as Evidence}from '@/lib/v2/assessmentProcessor';import{EvidencePanel}from'./EvidencePanel';
const labels={address_first:'Worth addressing first',understand:'Worth understanding',positive:'You’re in a good position here'}as const;
export function FindingCard({finding,evidence,showExplanation,showEvidence,index}:{finding:DisplayFinding;evidence?:Evidence;showExplanation:boolean;showEvidence:boolean;index:number}){return <article className="rounded-2xl border border-border/70 bg-card/80 p-5 shadow-sm sm:p-7"><div className="flex items-start gap-4"><span className="font-mono text-sm text-primary">{String(index+1).padStart(2,'0')}</span><div className="min-w-0 flex-1"><p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">{labels[finding.priority]}</p><h2 className="mt-2 text-2xl font-headline font-semibold leading-tight">{finding.title}</h2>{showExplanation&&<p className="mt-4 text-base leading-7 text-foreground/85">{finding.explanation}</p>}{showEvidence&&<EvidencePanel statements={evidence?.statements||[]}/>}</div></div></article>;}
