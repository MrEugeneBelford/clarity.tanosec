import type{ActionTemplate}from '@/lib/v2/findings';
const assistance:Record<ActionTemplate['type'],string>={self_help:'A practical step you can take internally',tanosec_service:'Tanosec can help if you want support',specialist:'Specialist help may be appropriate'};
export function NextActions({actions}:{actions:readonly ActionTemplate[]}){if(!actions.length)return null;return <section aria-labelledby="next-actions"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">Your next moves</p><h2 id="next-actions" className="mt-2 text-3xl font-headline font-semibold">Three useful places to start</h2><div className="mt-6 grid gap-4 md:grid-cols-3">{actions.slice(0,3).map((action,index)=><article key={action.id} className="rounded-2xl border border-border/70 bg-card/70 p-5"><span className="font-mono text-sm text-primary">{String(index+1).padStart(2,'0')}</span><h3 className="mt-4 text-lg font-semibold">{action.title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{action.description}</p><p className="mt-4 border-t border-border/50 pt-3 text-xs text-foreground/70">{assistance[action.type]}</p></article>)}</div></section>;}
