import type{ClaritySnapshot}from '@/lib/v2/assessmentProcessor';import{generateClaritySnapshotPdf}from '@/lib/v2/pdfReport';import{Button}from'@/components/ui/button';import{SnapshotHeader}from'./SnapshotHeader';import{FindingCard}from'./FindingCard';import{StrengthCard}from'./StrengthCard';import{NextActions}from'./NextActions';import{shouldShowExplanation}from'./visibility';
export function ResultsScreen({snapshot,unlocked,onRestart,onRequestFull}:{snapshot:ClaritySnapshot;unlocked:boolean;onRestart:()=>void;onRequestFull:()=>void}){const positives=snapshot.findings.filter(f=>f.priority==='positive');return <div className="w-full max-w-5xl space-y-12 py-6 sm:py-10"><SnapshotHeader summary={snapshot.businessContext.summary} onRestart={onRestart}/><section aria-labelledby="stand-out"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">What stands out</p><h2 id="stand-out" className="mt-2 text-3xl font-headline font-semibold">The clearest signals in your answers</h2><div className="mt-6 grid gap-5">{snapshot.findings.map((finding,index)=><FindingCard key={finding.id} finding={finding} index={index} evidence={snapshot.evidencePanels.find(panel=>panel.findingId===finding.id)} showExplanation={shouldShowExplanation(finding,index,unlocked)} showEvidence={unlocked}/>)}</div></section>{!unlocked&&<section className="rounded-2xl border border-primary/30 bg-primary/5 p-6 sm:p-8"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">Want the full picture?</p><h2 className="mt-2 text-2xl font-headline font-semibold">See the evidence and your next three moves</h2><p className="mt-3 max-w-2xl leading-7 text-muted-foreground">You already have the main finding and every result title. Add your email to see the remaining explanations, the evidence behind each finding, and the practical next actions.</p><Button className="mt-6" onClick={onRequestFull}>Send me my Clarity Snapshot</Button></section>}{unlocked&&<NextActions actions={snapshot.nextActions}/>} {unlocked&&positives.length>0&&<section aria-labelledby="strengths"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">What you’re doing well</p><h2 id="strengths" className="mt-2 text-3xl font-headline font-semibold">Foundations worth keeping</h2><div className="mt-6 grid gap-4 md:grid-cols-2">{positives.map(finding=><StrengthCard key={finding.id} finding={finding}/>)}</div></section>}{unlocked&&<footer className="border-t border-border/60 pt-8"><p className="max-w-3xl text-lg leading-8 text-foreground/80">{snapshot.closingNote}</p><div className="mt-7 flex flex-col items-start gap-3 sm:flex-row sm:items-center"><Button variant="outline" onClick={()=>void generateClaritySnapshotPdf(snapshot)}>Download PDF snapshot</Button><a href="https://calendly.com/tanosec" target="_blank" rel="noopener noreferrer"><Button variant="outline">Not sure what to do next? Let’s figure it out.</Button></a><span className="text-xs text-muted-foreground">Clarity is a starting point, not a security guarantee.</span></div></footer>}</div>;}
